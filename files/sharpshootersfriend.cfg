// Define here your key to sharpshoot
bindkey = "LSHIFT";

bind $bindkey [ // Let's go
	oldsens = $sensitivity;
	sensitivity 2.0;
	onrelease [ // If the player releases the button
		sensitivity $oldsens;
	];
];